# Data policy

Raw microscopy datasets are not included in this repository.

Do not upload `.lif`, `.czi`, `.nd2`, `.ims`, large `.tif/.tiff` stacks or private laboratory data. Keep raw data in institutional storage and document only lightweight examples or synthetic/minimal demonstration files if needed.
